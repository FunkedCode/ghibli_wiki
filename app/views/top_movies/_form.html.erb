<%= form_with(model: top_movie, local: true) do |form| %>
  <% if top_movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(top_movie.errors.count, "error") %> prohibited this top_movie from being saved:</h2>

      <ul>
      <% top_movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
