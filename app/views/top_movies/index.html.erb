<p id="notice"><%= notice %></p>

<h1>Top 150 Movies</h1>
<table>
  <thead>
    <tr>
      <th colspan="1"></th>
      <th>MAL Score</th>
    </tr>
  </thead>

  <tbody>
    <% @top_movies.each do |top_movie| %>
  <tr><th><%= link_to "#{top_movie.rank}. #{top_movie.title}", top_movie.mal_url,target: :_blank %></th></tr>
      <tr>
        <td>
          <img class="poster" src= <%=top_movie.image_url %> />
        </td>
        <td><%= top_movie.mal_score %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Top Movie', new_top_movie_path %>
